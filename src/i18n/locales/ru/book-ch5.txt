@@ ch.ydkjs-scope.ch5.title
(Не такой уж) тайный жизненный цикл переменных

@@ ch.ydkjs-scope.ch5.sum
Когда переменную можно использовать: hoisting функций и var, hoisting как метафора, повторное объявление, const, циклы и TDZ.

@@ sec.ydkjs-scope.ch5.intro.heading
Глава 5: Жизненный цикл переменных

@@ sec.ydkjs-scope.ch5.when-can-i-use.heading
Когда можно использовать переменную?

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.heading
Hoisting: объявление и выражение

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.note
Тип ошибки — инструмент диагностики:

```text
ReferenceError: x is not defined        → переменной нет вообще (undeclared)
ReferenceError: Cannot access x before… → есть, но в TDZ (let/const)
TypeError: x is not a function          → есть, но значение не функция (часто undefined)
```

В React, если использовать функцию, записанную как `const handleClick = () => {}`, выше её определения, получишь ошибку TDZ. А `function handleClick() {}` поднимается.

@@ sec.ydkjs-scope.ch5.variable-hoisting.heading
Hoisting переменных

@@ sec.ydkjs-scope.ch5.variable-hoisting.note
На вопрос «что такое hoisting?» на собеседовании давай ответ в два уровня:

1. **Просто:** объявления ведут себя так, будто их «подняли» в начало scope.
2. **Точно:** код никуда не перемещается — при компиляции находятся все объявления и генерируются инструкции для их регистрации при каждом входе в scope.

Второй ответ показывает уровень middle, потому что доказывает, что ты знаешь модель [[two-phases]].

@@ sec.ydkjs-scope.ch5.redeclaration.heading
Повторное объявление?

@@ sec.ydkjs-scope.ch5.redeclaration.note
Этот запрет защищает тебя в реальном проекте: если в большом компоненте написать два `const` с одним именем, сборка сразу упадёт — баг не дойдёт до продакшена.

Запомни: `var x;` **не сбрасывает** значение. А `let x;` — то же самое, что `let x = undefined;`, потому что `let` не инициализируется в начале scope.

@@ sec.ydkjs-scope.ch5.constants.heading
Константы?

@@ sec.ydkjs-scope.ch5.constants.note
Самый частый источник путаницы: `const` замораживает **привязку**, а не **значение**.

```js
const user = { name: 'Aysel' };
user.name = 'Leyla';   // можно — меняется содержимое объекта
user = {};             // TypeError — переменной нельзя присвоить новое значение
```

При работе со state в React это различие критично: изменение объекта на месте не вызывает перерендер — нужен новый объект ([[immutability]]).

@@ sec.ydkjs-scope.ch5.loops.heading
Циклы

@@ sec.ydkjs-scope.ch5.loops.note
Отсюда знаменитый вопрос с собеседований:

```js
for (var i = 0; i < 3; i++) setTimeout(() => console.log(i));
// 3 3 3 — все колбэки видят ОДНУ И ТУ ЖЕ i
for (let i = 0; i < 3; i++) setTimeout(() => console.log(i));
// 0 1 2 — у каждой итерации своя i
```

Причина — правило этого раздела: «каждая итерация — новый экземпляр scope». Глава 7 покажет это снова с точки зрения [[closure]].

@@ sec.ydkjs-scope.ch5.tdz.heading
Неинициализированные переменные (TDZ)

@@ sec.ydkjs-scope.ch5.tdz.note
Запомни TDZ как две отдельные операции:

```text
              регистрация (hoisting)   инициализация
var           в начале scope           в начале scope (undefined)
let / const   в начале scope           в строке объявления  ← между ними TDZ
function      в начале scope           в начале scope (функция)
```

В React: вызывать функцию, объявленную ниже через `const`, изнутри `useEffect` можно (эффект выполняется позже), но вызов во время рендера выше её объявления даст ошибку TDZ. Правило простое — сначала объяви, потом используй.

@@ sec.ydkjs-scope.ch5.finally-initialized.heading
Наконец-то инициализировано

@@ sec.ydkjs-scope.ch5.intro.b0
К этому моменту ты уже должен неплохо понимать вложенность областей видимости от глобальной области вниз — так называемую цепочку областей видимости программы.

@@ sec.ydkjs-scope.ch5.intro.b1
Но знать, из какой области пришла переменная, — лишь часть истории. Если объявление переменной находится после первого выражения области, как поведут себя обращения к этому идентификатору *до* объявления? Что будет, если попытаться объявить одну и ту же переменную в области дважды?

@@ sec.ydkjs-scope.ch5.intro.b2
Особая разновидность лексической области видимости в JS полна нюансов в том, как и когда переменные появляются и становятся доступны программе.

@@ sec.ydkjs-scope.ch5.when-can-i-use.b0
В какой момент переменная становится доступной для использования в своей области? Ответ может показаться очевидным: *после* того как переменная объявлена/создана. Верно? Не совсем.

@@ sec.ydkjs-scope.ch5.when-can-i-use.b1
Рассмотрим:

@@ sec.ydkjs-scope.ch5.when-can-i-use.b3
Этот код отлично работает. Возможно, ты видел или даже писал подобный код. Но задумывался ли ты, как или почему он работает? В частности, почему можно обратиться к идентификатору `greeting` в строке 1 (чтобы получить и выполнить ссылку на функцию), хотя объявление функции `greeting()` появляется только в строке 4?

@@ sec.ydkjs-scope.ch5.when-can-i-use.b4
Вспомни: в главе 1 отмечалось, что все идентификаторы регистрируются в своих областях во время компиляции. Более того, каждый идентификатор *создаётся* в начале области, которой он принадлежит, **каждый раз при входе в эту область**.

@@ sec.ydkjs-scope.ch5.when-can-i-use.b5
Термин, который чаще всего используют для того, что переменная видна с начала охватывающей её области, хотя её объявление может находиться ниже, — [[hoisting]].

@@ sec.ydkjs-scope.ch5.when-can-i-use.b6
Но одного hoisting недостаточно, чтобы полностью ответить на вопрос. Идентификатор `greeting` виден с начала области, но почему функцию `greeting()` можно *вызвать* до её объявления?

@@ sec.ydkjs-scope.ch5.when-can-i-use.b7
Иными словами, откуда у переменной `greeting` есть значение (ссылка на функцию) с самого момента запуска области? Ответ — особое свойство формальных объявлений `function`, называемое *function hoisting* ([[function-hoisting]]). Когда идентификатор имени объявления `function` регистрируется в начале области, он дополнительно автоматически инициализируется ссылкой на эту функцию. Поэтому функцию можно вызывать во всей области!

@@ sec.ydkjs-scope.ch5.when-can-i-use.b8
Одна ключевая деталь: и *function hoisting*, и *variable hoisting* в стиле `var` привязывают свои идентификаторы к ближайшей охватывающей **функциональной области** (или, если её нет, к глобальной), а не к блочной.

@@ sec.ydkjs-scope.ch5.when-can-i-use.b9
> **ПРИМЕЧАНИЕ:** Объявления `let` и `const` тоже поднимаются (см. обсуждение TDZ далее в этой главе). Но эти две формы объявления привязываются к охватывающему блоку, а не просто к охватывающей функции, как объявления `var` и `function`. Подробнее — в разделе «Области видимости с помощью блоков» главы 6.

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b0
*Function hoisting* применяется только к формальным объявлениям `function` (а именно к тем, что находятся вне блоков, — см. «FiB» в главе 6), но не к присваиваниям `function`-выражений. Рассмотрим:

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b2
Строка 1 (`greeting();`) выбрасывает ошибку. Но очень важно обратить внимание на *тип* выброшенной ошибки. `TypeError` означает, что мы пытаемся сделать со значением то, что недопустимо. В зависимости от JS-среды сообщение будет примерно таким: «'undefined' is not a function» или, что полезнее, «'greeting' is not a function».

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b3
Заметь: это **не** `ReferenceError`. JS не говорит, что не смог найти идентификатор `greeting` в области. Он говорит, что `greeting` найден, но в этот момент не содержит ссылку на функцию. Вызывать можно только функции, поэтому попытка вызвать значение, не являющееся функцией, приводит к ошибке.

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b4
Но что тогда содержит `greeting`, если не ссылку на функцию?

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b5
Переменные, объявленные через `var`, не только поднимаются, но и автоматически инициализируются значением `undefined` в начале своей области — опять же, ближайшей охватывающей функции или глобальной. После инициализации их можно использовать (присваивать, читать и т. д.) во всей области.

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b6
Так что в первой строке `greeting` существует, но содержит лишь значение по умолчанию `undefined`. Ссылку на функцию `greeting` получает только в строке 4.

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b7
Обрати пристальное внимание на это различие. Объявление `function` поднимается **и инициализируется значением-функцией** (снова: это называется *function hoisting*). Переменная `var` тоже поднимается, а затем автоматически инициализируется значением `undefined`. Любые последующие присваивания этой переменной `function`-выражений происходят только тогда, когда это присваивание обрабатывается во время выполнения.

@@ sec.ydkjs-scope.ch5.declaration-vs-expression.b8
В обоих случаях поднимается имя идентификатора. Но связь со ссылкой на функцию не устанавливается во время инициализации (в начале области), если только идентификатор не был создан формальным объявлением `function`.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b0
Рассмотрим ещё один пример *variable hoisting*:

@@ sec.ydkjs-scope.ch5.variable-hoisting.b2
Хотя `greeting` объявляется только в строке 5, присвоить ей значение можно уже в строке 1. Почему?

@@ sec.ydkjs-scope.ch5.variable-hoisting.b3
У объяснения две необходимые части:

- идентификатор поднимается,
- **и** он автоматически инициализируется значением `undefined` с начала области.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b4
> **ПРИМЕЧАНИЕ:** Такое использование *variable hoisting*, вероятно, кажется неестественным, и многие читатели вполне обоснованно захотят не полагаться на него в своих программах. Но нужно ли избегать всего hoisting (включая *function hoisting*)? Эти разные взгляды на hoisting мы подробнее разберём в Приложении A.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b5
## Hoisting: ещё одна метафора

@@ sec.ydkjs-scope.ch5.variable-hoisting.b6
Глава 2 была полна метафор (для иллюстрации scope), а здесь мы сталкиваемся с ещё одной: самим hoisting. Вместо того чтобы считать hoisting конкретным шагом выполнения, который делает JS-движок, полезнее думать о нём как о визуализации различных действий, которые JS выполняет, подготавливая программу **до выполнения**.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b7
Типичное утверждение о том, что означает hoisting: *подъём* — как подъём тяжёлого груза — любых идентификаторов на самый верх области. Часто утверждается, что JS-движок перед выполнением действительно *переписывает* программу, и она выглядит примерно так:

@@ sec.ydkjs-scope.ch5.variable-hoisting.b9
Метафора hoisting предполагает, что JS предварительно обрабатывает исходную программу и немного её переставляет, так что до выполнения все объявления оказываются наверху своих областей. Более того, метафора hoisting утверждает, что объявления `function` поднимаются наверх каждой области целиком. Рассмотрим:

@@ sec.ydkjs-scope.ch5.variable-hoisting.b11
«Правило» метафоры hoisting: сначала поднимаются объявления функций, а сразу после всех функций — переменные. Таким образом, история о hoisting предполагает, что JS-движок *переставляет* программу так:

@@ sec.ydkjs-scope.ch5.variable-hoisting.b13
Эта метафора hoisting удобна. Её преимущество в том, что она позволяет отмахнуться от волшебной предварительной обработки «с заглядыванием вперёд», необходимой, чтобы найти все эти объявления, спрятанные глубоко в областях, и как-то переместить (поднять) их наверх; мы можем просто думать о программе так, будто JS-движок выполняет её **за один проход** сверху вниз.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b14
Один проход определённо кажется проще, чем утверждение главы 1 о двухэтапной обработке.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b15
Hoisting как механизм перестановки кода может быть привлекательным упрощением, но он неточен. JS-движок на самом деле не переставляет код. Он не может волшебным образом заглянуть вперёд и найти объявления; единственный способ точно найти их, а также все границы областей в программе, — полностью распарсить код.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b16
Угадай, что такое парсинг? Первый этап двухэтапной обработки! Никакая волшебная умственная гимнастика не обходит этот факт.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b17
Так если метафора hoisting (в лучшем случае) неточна, что делать с этим термином? Думаю, он по-прежнему полезен — его регулярно используют даже члены TC39! — но не стоит утверждать, что это настоящая перестановка исходного кода.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b18
> **ПРЕДУПРЕЖДЕНИЕ:** Неверные или неполные ментальные модели часто всё же кажутся достаточными, потому что иногда случайно приводят к правильным ответам. Но в долгосрочной перспективе труднее точно анализировать и предсказывать результаты, если твоё мышление не согласуется с тем, как работает JS-движок.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b19
Я утверждаю, что термин hoisting *следует* использовать для **операции времени компиляции** — генерации инструкций для автоматической регистрации переменной в начале её области при каждом входе в эту область.

@@ sec.ydkjs-scope.ch5.variable-hoisting.b20
Это тонкий, но важный сдвиг: от hoisting как поведения во время выполнения к его законному месту среди задач времени компиляции.

@@ sec.ydkjs-scope.ch5.redeclaration.b0
Как ты думаешь, что происходит, когда переменная объявляется в одной области больше одного раза? Рассмотрим:

@@ sec.ydkjs-scope.ch5.redeclaration.b2
Что, по-твоему, напечатается во втором сообщении? Многие считают, что второй `var studentName` повторно объявил переменную (и тем самым «сбросил» её), поэтому ожидают, что напечатается `undefined`.

@@ sec.ydkjs-scope.ch5.redeclaration.b3
Но существует ли вообще такое понятие, как «повторное объявление» переменной в той же области? Нет.

@@ sec.ydkjs-scope.ch5.redeclaration.b4
Если рассматривать эту программу с точки зрения метафоры hoisting, код для выполнения был бы переставлен так:

@@ sec.ydkjs-scope.ch5.redeclaration.b6
Поскольку hoisting на самом деле связан с регистрацией переменной в начале области, в середине области, где в исходной программе было второе выражение `var studentName`, делать нечего. Это просто пустая операция (no-op), бессмысленное выражение.

@@ sec.ydkjs-scope.ch5.redeclaration.b7
> **СОВЕТ:** В стиле разговоров из главы 2: *Компилятор* нашёл бы второе объявление `var` и спросил бы *Scope Manager*, видел ли тот уже идентификатор `studentName`; поскольку видел, больше делать было бы нечего.

@@ sec.ydkjs-scope.ch5.redeclaration.b8
Важно также отметить, что `var studentName;` не означает `var studentName = undefined;`, как полагает большинство. Докажем, что это разные вещи, рассмотрев такой вариант программы:

@@ sec.ydkjs-scope.ch5.redeclaration.b10
Видишь, как явная инициализация `= undefined` даёт другой результат, чем предположение, что она неявно происходит, когда её опускают? В следующем разделе мы вернёмся к теме инициализации переменных при объявлении.

@@ sec.ydkjs-scope.ch5.redeclaration.b11
Повторное объявление `var` с тем же именем идентификатора в области фактически ничего не делает. Вот ещё одна иллюстрация, на этот раз с функцией того же имени:

@@ sec.ydkjs-scope.ch5.redeclaration.b13
Первое объявление `greeting` регистрирует идентификатор в области, и поскольку это `var`, автоматической инициализацией будет `undefined`. Объявлению `function` не нужно повторно регистрировать идентификатор, но из-за *function hoisting* оно переопределяет автоматическую инициализацию, используя ссылку на функцию. Второй `var greeting` сам по себе ничего не делает, так как `greeting` уже является идентификатором, а *function hoisting* уже получил приоритет при автоматической инициализации.

@@ sec.ydkjs-scope.ch5.redeclaration.b14
Реальное присваивание `"Hello!"` переменной `greeting` меняет её значение с исходной `function greeting()` на строку; сам `var` никакого эффекта не имеет.

@@ sec.ydkjs-scope.ch5.redeclaration.b15
А что, если повторить объявление в области с помощью `let` или `const`?

@@ sec.ydkjs-scope.ch5.redeclaration.b17
Эта программа не выполнится, а сразу выбросит `SyntaxError`. В зависимости от JS-среды сообщение будет примерно таким: «studentName has already been declared». Иными словами, это случай, когда попытка «повторного объявления» явно запрещена!

@@ sec.ydkjs-scope.ch5.redeclaration.b18
Эту ошибку выбрасывают не только два объявления с `let`. Если хотя бы одно объявление использует `let`, другое может быть как `let`, так и `var` — ошибка всё равно возникнет, как показывают эти два варианта:

@@ sec.ydkjs-scope.ch5.redeclaration.b20
и:

@@ sec.ydkjs-scope.ch5.redeclaration.b22
В обоих случаях `SyntaxError` выбрасывается на *втором* объявлении. Иными словами, единственный способ «повторно объявить» переменную — использовать `var` во всех (двух или более) её объявлениях.

@@ sec.ydkjs-scope.ch5.redeclaration.b23
Но зачем это запрещать? Причина ошибки не техническая как таковая, ведь «повторное объявление» `var` всегда было разрешено; очевидно, такое же разрешение можно было дать и `let`.

@@ sec.ydkjs-scope.ch5.redeclaration.b24
Это скорее вопрос «социальной инженерии». «Повторное объявление» переменных некоторые, включая многих членов TC39, считают плохой привычкой, которая может приводить к багам. Поэтому, вводя `let` в ES6, решили предотвращать «повторное объявление» ошибкой.

@@ sec.ydkjs-scope.ch5.redeclaration.b25
> **ПРИМЕЧАНИЕ:** Это, конечно, стилистическое мнение, а не технический аргумент. Многие разработчики согласны с этой позицией, и, вероятно, отчасти поэтому TC39 добавил ошибку (а также то, что `let` соответствует `const`). Но можно было обоснованно утверждать, что последовательность с прецедентом `var` была бы разумнее, а навязывание мнений лучше оставить опциональным инструментам вроде линтеров. В Приложении A мы разберём, может ли `var` (и связанное с ним поведение, вроде «повторного объявления») быть полезным в современном JS.

@@ sec.ydkjs-scope.ch5.redeclaration.b26
Когда *Компилятор* спрашивает *Scope Manager* об объявлении и этот идентификатор уже объявлен, причём одно или оба объявления сделаны через `let`, выбрасывается ошибка. Задуманный сигнал разработчику: «Хватит полагаться на неряшливое повторное объявление!»

@@ sec.ydkjs-scope.ch5.constants.b0
Ключевое слово `const` ограничено сильнее, чем `let`. Как и `let`, `const` нельзя повторить с тем же идентификатором в той же области. Но на самом деле есть весомая техническая причина запрета такого «повторного объявления» — в отличие от `let`, который запрещает его в основном по стилистическим причинам.

@@ sec.ydkjs-scope.ch5.constants.b1
Ключевое слово `const` требует инициализации переменной, поэтому пропуск присваивания в объявлении приводит к `SyntaxError`:

@@ sec.ydkjs-scope.ch5.constants.b3
Объявления `const` создают переменные, которые нельзя переприсвоить:

@@ sec.ydkjs-scope.ch5.constants.b5
Переменную `studentName` нельзя переприсвоить, потому что она объявлена через `const`.

@@ sec.ydkjs-scope.ch5.constants.b6
> **ПРЕДУПРЕЖДЕНИЕ:** При переприсваивании `studentName` выбрасывается `TypeError`, а не `SyntaxError`. Это тонкое различие на самом деле довольно важно, но, к сожалению, его слишком легко упустить. Синтаксические ошибки — это дефекты программы, которые не дают ей даже начать выполнение. Ошибки типа — это дефекты, возникающие во время выполнения. В предыдущем фрагменте `"Frank"` печатается до того, как мы обрабатываем переприсваивание `studentName`, которое затем и выбрасывает ошибку.

@@ sec.ydkjs-scope.ch5.constants.b7
Итак, если объявления `const` нельзя переприсвоить и объявления `const` всегда требуют присваивания, то есть ясная техническая причина, почему `const` должен запрещать любые «повторные объявления»: любое «повторное объявление» `const` обязательно было бы и переприсваиванием `const`, что недопустимо!

@@ sec.ydkjs-scope.ch5.constants.b9
Поскольку «повторное объявление» `const` должно быть запрещено (по этим техническим причинам), TC39, по сути, решил, что ради единообразия нужно запретить и «повторное объявление» `let`. Спорно, был ли это лучший выбор, но по крайней мере мы знаем логику решения.

@@ sec.ydkjs-scope.ch5.loops.b0
Из предыдущего обсуждения ясно, что JS не особенно хочет, чтобы мы «повторно объявляли» переменные в одной области. Это кажется простым наставлением — пока не задумаешься, что это значит для повторного выполнения объявлений в циклах. Рассмотрим:

@@ sec.ydkjs-scope.ch5.loops.b2
Повторно ли «объявляется» `value` в этой программе? Будут ли выброшены ошибки? Нет.

@@ sec.ydkjs-scope.ch5.loops.b3
Все правила области видимости (включая «повторное объявление» переменных, созданных через `let`) применяются *к каждому экземпляру области*. Иными словами, каждый раз при входе в область во время выполнения всё сбрасывается.

@@ sec.ydkjs-scope.ch5.loops.b4
Каждая итерация цикла — свой новый экземпляр области, и в каждом экземпляре `value` объявляется только один раз. Поэтому попытки «повторного объявления» нет, а значит, нет и ошибки. Прежде чем рассмотреть другие формы циклов: что, если объявление `value` в предыдущем фрагменте заменить на `var`?

@@ sec.ydkjs-scope.ch5.loops.b6
«Повторно объявляется» ли `value` здесь, особенно учитывая, что `var` это разрешает? Нет. Поскольку `var` не считается блочным объявлением (см. главу 6), он привязывается к глобальной области. Так что есть только одна переменная `value` — в той же области, что и `keepGoing` (в данном случае глобальной). И здесь нет «повторного объявления»!

@@ sec.ydkjs-scope.ch5.loops.b7
Один из способов не запутаться — помнить, что к моменту начала выполнения ключевые слова `var`, `let` и `const` фактически *удалены* из кода. Их целиком обрабатывает компилятор.

@@ sec.ydkjs-scope.ch5.loops.b8
Если мысленно стереть ключевые слова объявлений и попробовать обработать код, это поможет понять, происходят ли (повторные) объявления и когда.

@@ sec.ydkjs-scope.ch5.loops.b9
А как насчёт «повторного объявления» в других формах циклов, например в циклах `for`?

@@ sec.ydkjs-scope.ch5.loops.b11
Должно быть ясно, что в каждом экземпляре области объявляется только одна `value`. А что с `i`? Она «повторно объявляется»?

@@ sec.ydkjs-scope.ch5.loops.b12
Чтобы ответить, подумай, в какой области находится `i`. Может показаться, что во внешней (в данном случае глобальной), но это не так. Она находится в области тела цикла `for`, как и `value`. Собственно, этот цикл можно представить в такой более многословной эквивалентной форме:

@@ sec.ydkjs-scope.ch5.loops.b14
Теперь должно быть ясно: и `i`, и `value` объявляются ровно один раз **в каждом экземпляре области**. Никакого «повторного объявления».

@@ sec.ydkjs-scope.ch5.loops.b15
А другие формы цикла `for`?

@@ sec.ydkjs-scope.ch5.loops.b17
То же самое с циклами `for..in` и `for..of`: объявленная переменная считается находящейся *внутри* тела цикла и поэтому обрабатывается на каждой итерации (то есть в каждом экземпляре области). Никакого «повторного объявления».

@@ sec.ydkjs-scope.ch5.loops.b18
Ладно, знаю, ты думаешь, что я звучу как заезженная пластинка. Но давай разберём, как `const` влияет на эти циклические конструкции. Рассмотрим:

@@ sec.ydkjs-scope.ch5.loops.b20
Как и в варианте этой программы с `let`, который мы видели ранее, `const` выполняется ровно один раз на каждой итерации, так что проблем с «повторным объявлением» нет. Но с циклами `for` всё сложнее.

@@ sec.ydkjs-scope.ch5.loops.b21
`for..in` и `for..of` можно спокойно использовать с `const`:

@@ sec.ydkjs-scope.ch5.loops.b23
Но не обычный цикл `for`:

@@ sec.ydkjs-scope.ch5.loops.b25
Что здесь не так? В этой конструкции можно было спокойно использовать `let`, и мы утверждали, что он создаёт новую `i` для области каждой итерации, так что это даже не похоже на «повторное объявление».

@@ sec.ydkjs-scope.ch5.loops.b26
Давай мысленно «развернём» этот цикл, как делали раньше:

@@ sec.ydkjs-scope.ch5.loops.b28
Видишь проблему? Наша `i` действительно создаётся внутри цикла только один раз. Проблема не в этом. Проблема в концептуальной `$$i`, которую нужно каждый раз увеличивать выражением `$$i++`. Это **переприсваивание** (а не «повторное объявление»), а для констант оно запрещено.

@@ sec.ydkjs-scope.ch5.loops.b29
Помни, эта «развёрнутая» форма — лишь концептуальная модель, помогающая интуитивно понять источник проблемы. Можно задаться вопросом: не мог ли JS фактически превратить `const $$i = 0` в `let $ii = 0`, чтобы `const` работал с классическим циклом `for`? Возможно, но тогда это могло бы внести потенциально неожиданные исключения в семантику цикла `for`.

@@ sec.ydkjs-scope.ch5.loops.b30
Например, было бы довольно произвольным (и, вероятно, запутанным) исключением позволить `i++` в заголовке цикла `for` обходить строгость присваивания `const`, но не разрешать другие переприсваивания `i` внутри итерации цикла, которые иногда бывают полезны.

@@ sec.ydkjs-scope.ch5.loops.b31
Простой ответ: `const` нельзя использовать с классической формой цикла `for` из-за необходимого переприсваивания.

@@ sec.ydkjs-scope.ch5.loops.b32
Интересно, что если не делать переприсваивания, то это допустимо:

@@ sec.ydkjs-scope.ch5.loops.b34
Это работает, но бессмысленно. Нет никакой причины объявлять `i` в этой позиции через `const`, ведь весь смысл такой переменной в этой позиции — **использоваться для подсчёта итераций**. Просто используй другую форму цикла, например `while`, или `let`!

@@ sec.ydkjs-scope.ch5.tdz.b0
При объявлениях `var` переменная «поднимается» наверх своей области. Но она ещё и автоматически инициализируется значением `undefined`, чтобы её можно было использовать во всей области.

@@ sec.ydkjs-scope.ch5.tdz.b1
Однако объявления `let` и `const` в этом отношении не совсем такие же.

@@ sec.ydkjs-scope.ch5.tdz.b2
Рассмотрим:

@@ sec.ydkjs-scope.ch5.tdz.b4
Результат этой программы — `ReferenceError` в первой строке. В зависимости от JS-среды сообщение может быть примерно таким: «Cannot access studentName before initialization».

@@ sec.ydkjs-scope.ch5.tdz.b5
> **ПРИМЕЧАНИЕ:** Раньше это сообщение об ошибке было гораздо более расплывчатым или вводящим в заблуждение. К счастью, нескольким из нас в сообществе удалось добиться, чтобы JS-движки улучшили это сообщение и оно точнее говорило, что не так!

@@ sec.ydkjs-scope.ch5.tdz.b6
Это сообщение довольно точно указывает, в чём проблема: `studentName` существует в строке 1, но не инициализирована, поэтому её пока нельзя использовать. Попробуем так:

@@ sec.ydkjs-scope.ch5.tdz.b8
Упс. Мы всё равно получаем `ReferenceError`, но теперь в первой строке, где пытаемся присвоить значение (то есть инициализировать!) этой так называемой «неинициализированной» переменной `studentName`. В чём дело!?

@@ sec.ydkjs-scope.ch5.tdz.b9
Настоящий вопрос: как инициализировать неинициализированную переменную? Для `let`/`const` **единственный способ** — присваивание, привязанное к объявлению. Одного присваивания недостаточно! Рассмотрим:

@@ sec.ydkjs-scope.ch5.tdz.b11
Здесь мы инициализируем `studentName` (в данном случае значением `"Suzy"` вместо `undefined`) с помощью формы объявления `let`, объединённой с присваиванием.

@@ sec.ydkjs-scope.ch5.tdz.b12
Или так:

@@ sec.ydkjs-scope.ch5.tdz.b14
> **ПРИМЕЧАНИЕ:** Интересно! Вспомни: раньше мы говорили, что `var studentName;` — *не* то же самое, что `var studentName = undefined;`, а здесь, с `let`, они ведут себя одинаково. Разница в том, что `var studentName` автоматически инициализируется в начале области, а `let studentName` — нет.

@@ sec.ydkjs-scope.ch5.tdz.b15
Помни, мы уже несколько раз утверждали, что *Компилятор* в итоге удаляет все объявления `var`/`let`/`const`, заменяя их инструкциями в начале каждой области, которые регистрируют соответствующие идентификаторы.

@@ sec.ydkjs-scope.ch5.tdz.b16
Если проанализировать, что здесь происходит, видно дополнительный нюанс: *Компилятор* также добавляет инструкцию в середину программы, в точку, где была объявлена переменная `studentName`, чтобы обработать автоматическую инициализацию этого объявления. Мы не можем использовать переменную ни в какой момент до этой инициализации. Для `const` это верно так же, как для `let`.

@@ sec.ydkjs-scope.ch5.tdz.b17
Термин, придуманный TC39 для этого *промежутка времени* от входа в область до автоматической инициализации переменной: Temporal Dead Zone ([[tdz]]).

@@ sec.ydkjs-scope.ch5.tdz.b18
TDZ — это временное окно, когда переменная существует, но ещё не инициализирована и поэтому недоступна никаким образом. Инициализировать её может только выполнение инструкций, оставленных *Компилятором* в месте исходного объявления. После этого момента TDZ заканчивается, и переменную можно свободно использовать в оставшейся части области.

@@ sec.ydkjs-scope.ch5.tdz.b19
Технически у `var` тоже есть TDZ, но её длина равна нулю, и поэтому она не наблюдаема в наших программах! Наблюдаемая TDZ есть только у `let` и `const`.

@@ sec.ydkjs-scope.ch5.tdz.b20
Кстати, «temporal» в TDZ действительно относится ко *времени*, а не к *положению в коде*. Рассмотрим:

@@ sec.ydkjs-scope.ch5.tdz.b22
Хотя по положению `console.log(..)` с обращением к `studentName` идёт *после* объявления `let studentName`, по времени функция `askQuestion()` вызывается *до* того, как встречается выражение `let`, пока `studentName` ещё в TDZ! Отсюда и ошибка.

@@ sec.ydkjs-scope.ch5.tdz.b23
Распространено заблуждение, что TDZ означает, будто `let` и `const` не поднимаются. Это неточное или по меньшей мере слегка вводящее в заблуждение утверждение. Они определённо поднимаются.

@@ sec.ydkjs-scope.ch5.tdz.b24
Настоящее различие в том, что объявления `let`/`const` не инициализируются автоматически в начале области, как `var`. Тогда *спор* идёт о том, является ли автоматическая инициализация *частью* hoisting или нет? Я считаю, что автоматическая регистрация переменной в начале области (то, что я называю «hoisting») и автоматическая инициализация в начале области (значением `undefined`) — разные операции, и их не стоит объединять под одним термином «hoisting».

@@ sec.ydkjs-scope.ch5.tdz.b25
Мы уже видели, что `let` и `const` не инициализируются автоматически в начале области. Но давай *докажем*, что `let` и `const` *поднимаются* (автоматически регистрируются в начале области), с помощью нашего друга — затенения (см. «Shadowing» в главе 3):

@@ sec.ydkjs-scope.ch5.tdz.b27
Что произойдёт с первым выражением `console.log(..)`? Если бы `let studentName` не поднимался наверх области, первый `console.log(..)` *должен* был бы напечатать `"Kyle"`, верно? Казалось бы, в этот момент существует только внешняя `studentName`, значит, именно к ней `console.log(..)` и должен обратиться и напечатать её.

@@ sec.ydkjs-scope.ch5.tdz.b28
Но вместо этого первый `console.log(..)` выбрасывает ошибку TDZ, потому что на самом деле `studentName` внутренней области **была** поднята (автоматически зарегистрирована в начале области). Чего **не** произошло (пока!), так это автоматической инициализации этой внутренней `studentName`; в этот момент она ещё не инициализирована, отсюда и нарушение TDZ!

@@ sec.ydkjs-scope.ch5.tdz.b29
Подводя итог: ошибки TDZ возникают потому, что объявления `let`/`const` *поднимают* свои объявления наверх областей, но, в отличие от `var`, откладывают автоматическую инициализацию своих переменных до того момента в последовательности кода, где стояло исходное объявление. Это временное окно (подсказка: temporal), какой бы длины оно ни было, и есть TDZ.

@@ sec.ydkjs-scope.ch5.tdz.b30
Как избежать ошибок TDZ?

@@ sec.ydkjs-scope.ch5.tdz.b31
Мой совет: всегда помещай объявления `let` и `const` в начало любой области. Сократи окно TDZ до нулевой (или почти нулевой) длины — и оно потеряет значение.

@@ sec.ydkjs-scope.ch5.tdz.b32
Но зачем вообще нужна TDZ? Почему TC39 не предписал `let`/`const` автоматически инициализироваться, как `var`? Потерпи, мы вернёмся к вопросу *почему* существует TDZ в Приложении A.

@@ sec.ydkjs-scope.ch5.finally-initialized.b0
Работа с переменными гораздо тоньше, чем кажется на первый взгляд. Hoisting, (повторное) объявление и TDZ — частые источники путаницы для разработчиков, особенно для тех, кто до JS работал с другими языками. Прежде чем двигаться дальше, убедись, что твоя ментальная модель прочно опирается на эти аспекты областей видимости и переменных JS.

@@ sec.ydkjs-scope.ch5.finally-initialized.b1
Hoisting обычно называют явным механизмом JS-движка, но на самом деле это скорее метафора, описывающая разные способы, которыми JS обрабатывает объявления переменных во время компиляции. Но даже как метафора hoisting даёт полезную структуру для размышлений о жизненном цикле переменной — когда она создаётся, когда становится доступной, когда исчезает.

@@ sec.ydkjs-scope.ch5.finally-initialized.b2
Объявление и повторное объявление переменных обычно вызывают путаницу, если думать о них как об операциях времени выполнения. Но если перейти к мышлению в терминах времени компиляции, странностей и теней становится меньше.

@@ sec.ydkjs-scope.ch5.finally-initialized.b3
Ошибка TDZ (temporal dead zone) при встрече кажется странной и раздражающей. К счастью, TDZ относительно легко избежать, если всегда аккуратно размещать объявления `let`/`const` в начале любой области.

@@ sec.ydkjs-scope.ch5.finally-initialized.b4
Пока ты успешно проходишь эти повороты области видимости переменных, следующая глава изложит факторы, которые определяют наши решения о размещении объявлений в разных областях, особенно во вложенных блоках.
